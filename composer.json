{

    "require": {

        "vlucas/phpdotenv": "^5.6",
        "guzzlehttp/guzzle": "^7.9",
        "openai-php/client": "^0.13.0"

    },
    "config": {
        "allow-plugins": {
            "php-http/discovery": true
        }
    }
}
